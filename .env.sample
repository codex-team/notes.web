# Notes API url
VITE_API_URL=http://localhost:3000

# Notes default production hostname
VITE_PRODUCTION_HOSTNAME=notex.so

# Google OAuth redirect url
VITE_GOOGLE_OAUTH_URL=https://api.notex.so/oauth/google/login

# Wallet connect project id
VITE_WALLET_CONNECT_PROJECT_ID=

# Payment gateway url used for aceessing transaction data
VITE_PAYMENT_GATEWAY_URL=
